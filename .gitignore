# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
.next.bak.*

# production
/build

# vercel
.vercel

# local tooling
.claude/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# GraphQL Codegen output is COMMITTED (Vercel can't run codegen during the
# build, and the app imports typed documents from here — so it must be in the
# repo). Regenerate with `npm run codegen` when the WordPress schema changes.

.vercel
.env*
